# docgrep

共通規約: [../ws-conventions/README.md](../ws-conventions/README.md) に従う（`~/ws` 配下の全リポジトリ共通）。

各リポジトリ固有の事情は本ファイルに追記する。

## commit / push の方針（共通規約 A からの逸脱）

**本リポジトリでは、修正のたびに自動でコミット・プッシュする。** 利用者の指示による（2026-09-21）。

共通規約 A は「commit / push は、利用者が明示的に指示したときだけ実行する」と定めるが、本リポジトリでは毎回の確認を省く。**黙って逸脱しない**ために本節を置く。

運用は次のとおり。

- **検証を通してからコミットする。** 規約 D の検査（`check-markdown.sh` / `check-privacy.sh`）と `pytest` が通らないうちはコミットしない
- **意味のある単位で分ける。** 無関係な修正をまとめない。提案・不具合・機能ごとに 1 コミット
- **コミットの直後に push する。** ローカルとリモートが一致することを確認する
- **履歴を書き換える操作は対象外。** `rebase` / `commit --amend` / `push --force` / ブランチの削除は、従来どおり明示の指示があるときだけ行う
